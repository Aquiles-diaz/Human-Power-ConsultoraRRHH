# requirements.txt
# Versiones mínimas con wheels para Python 3.13/3.14 (las versiones viejas
# fijadas no compilan en 3.14: pydantic-core/PyO3 no lo soportaban).
# El backend hashea con pbkdf2_sha256 (passlib), no usa bcrypt.
fastapi>=0.136
# starlette>=1.3.1: parchea PYSEC-2026-248 (path validation manipula url.hostname)
# y PYSEC-2026-249 (límites de parseo de forms ignorados en x-www-form-urlencoded).
starlette>=1.3.1
# cryptography>=48.0.1: parchea GHSA-537c-gmf6-5ccf (out-of-bounds read en OpenSSL).
cryptography>=48.0.1
uvicorn[standard]>=0.49
passlib>=1.7.4
python-jose[cryptography]>=3.5
python-multipart>=0.0.32
pydantic[email]>=2.13
# Migración a Supabase: Postgres (psycopg v3) + Storage (supabase-py).
psycopg[binary]>=3.2
# Pool de conexiones: antes se abría una conexión nueva (TCP+TLS+auth contra
# Supabase en otra región) por request, y endpoints como /apply abren tres.
psycopg-pool>=3.2
supabase>=2.0
python-dotenv>=1.0
# Rate limiting (protección de fuerza bruta en login/register).
slowapi>=0.1.9
# Fotos de perfil: se guardan como avatar (<=512 px WebP) en vez de crudas. Entraban
# hasta 5 MB para mostrarse a 56 px y la grilla de Candidatos bajaba ~140 MB por
# carga: eso agotó la cuota de egress del plan Free (ver _shrink_image en main.py).
Pillow>=11
# Login con Google: verificación del ID token (endpoint /auth/google).
# El extra [requests] trae la librería `requests`, que usa
# google.auth.transport.requests para validar el token. Sin ella, /auth/google
# revienta con ModuleNotFoundError: No module named 'requests'.
google-auth[requests]>=2.0
