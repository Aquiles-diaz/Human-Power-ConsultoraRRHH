[pytest]
# Agrega la raíz del repo a sys.path para que los tests puedan hacer
# `from backend.main import ...` al correr `pytest backend/tests` (sin depender de
# PYTHONPATH=. ni de `python -m`). Necesario en CI, que invoca pytest pelado.
pythonpath = .
