# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# Python
__pycache__/
*.pyc
*.pyo
*.pyd
dev.db
uploads/
# Respaldo local de scripts/shrink-photos.py: son fotos de personas reales
# bajadas del bucket. Nunca versionar.
backup-fotos/
# ── Secretos / variables de entorno (NUNCA versionar credenciales) ──
.env
.env.*
!.env.example
backend/.env
# El virtualenv vive en la raíz: los scripts de package.json (npm run backend,
# npm run test:api) invocan .venv/bin/python. Se ignoran también las variantes
# dentro de backend/ por si alguien lo crea ahí.
.venv/
venv/
backend/.venv/
backend/venv/
# Backend
backend/storage/uploads
backend/data.db
backend/data.db-shm
backend/data.db-wal
/data.db

# Build artifacts (no versionar)
*.tsbuildinfo
# Output compilado de vite.config.ts (Vite usa el .ts; estos confunden)
vite.config.js
vite.config.d.ts
.vercel
# Mockups del compañero visual de brainstorming
.superpowers/
# Docs internos de desarrollo (specs/planes); no van al repo público
docs/superpowers/
# Reportes de QA/seguridad: listan hallazgos; NO publicar en repo público
docs/QA-predeploy-*.md
# Config local de Claude Code: incluye worktrees (copias completas del repo).
# No versionar; un git add -A distraído los commitearía enteros.
.claude/
